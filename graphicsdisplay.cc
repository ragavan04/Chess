// #include "graphicsdisplay.h"
#include "board.h"
#include "piece.h"

// void GraphicsDisplay::notify(const Board &board) {
//     // auto state = board.getState();  // Get the 2D vector of piece pointers

//     // // Draw checkerboard pattern
//     // for (int i = 0; i < gridSize; ++i) {
//     //     for (int j = 0; j < gridSize; ++j) {
//     //         int colorIndex = (i + j) % 2 == 0 ? 3 : 0;  // Alternate colors for checkerboard
//     //         window.fillRectangle(i * dimentions, j * dimentions, dimentions, dimentions, colorIndex);
//     //     }
//     // }

//     // // Draw pieces on the board
//     // for (int i = 0; i < gridSize; ++i) {
//     //     for (int j = 0; j < gridSize; ++j) {
//     //         Piece *piece = state[i][j];
//     //         if (piece != nullptr) {
//     //             // You can modify this part to get the actual character representation of the piece
//     //             char pieceChar = piece->displayChar();  
//     //             string pieceStr(1, pieceChar);  // Convert char to string for drawing

//     //             // Draw the piece character in the middle of the square
//     //             int x = i * dimentions + dimentions / 2;
//     //             int y = j * dimentions + dimentions / 2;
//     //             window.drawString(y, x, pieceStr);
//     //         }
//     //     }
//     // }
// }

